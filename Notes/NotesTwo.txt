install npm by hitting cmd npm init or npm init -y to skip all the ques.
npm is used to manage our packager.
why we're using npm in our project?
bcz our react app powered by alot of things lot of packages suppose we have to minify it suppose we have to bundled things suppose we have to remove console.log
suppose we have to optimize our app so for all these we need alot of helper packages and those helper packages comes inside npm.

// install parcel 2nd step
cmd npm install -D parcel (read about dev dependency)
we use -D (hypen d) when we want our package on developer machine not on production.

read about package.json and package-lock.json
read about caret and tilde in package.json

// install react package
by hitting npm install react (and this is the right way)
imp* react comes as a package also.

// install react-dom package
by hitting npm install react-dom

// ignite our app 
by hitting npx parcel index.html (index.html is the entry point)
npx means execute using npm

/*
HMR - (hot module replacement)
File Watcher Algo - c++
Bundling
minify
Cleaning our code
Dev and production build
Super fast build algo
Image optimization
Caching while develoment
Compression
Compatable with older version of browser
HTTPS on dev
port number
Consistent Hashing Algo
Zero Configuation
*/


read about:
Transitive dependencies,
browserslist. (https://browserslist.dev/?q=bGFzdCAyIHZlcnNpb25z)

